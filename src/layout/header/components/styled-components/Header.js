import styled from 'styled-components'

export const Header = styled.div`
  position: relative;
  height: 3em;
  line-height: 3em;
  font-size: 1.2em;
  color: #fff;
  padding: 0 0.4em 0 0.4em;
  background-color: #E33080;
  overflow: hidden;
`

export const Logo = styled.img`
  position: relative;
  height: 2.8em;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`

export const RightFlow = styled.div`
  float: right;
`
